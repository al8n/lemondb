use skl::full::{sync::SkipMap, FullMap};

use crate::types::value::PhantomValue;

// /// Immutable log with multiple version support.
// pub struct ImmutableLog<K, V>
// where
//   K: ?Sized,
//   V: ?Sized,
// {
//   log: SkipMap<K, PhantomValue<V>, ImmutableMeta>,
// }
